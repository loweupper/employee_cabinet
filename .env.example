# ======================================
# 🔐 SECURITY / БЕЗОПАСНОСТЬ
# ======================================

# Генерируется командой: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=<ГЕНЕРИРУЕТСЯ_АВТОМАТИЧЕСКИ>

# ======================================
# 📊 DATABASE / БАЗА ДАННЫХ
# ======================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=employee_cabinet
POSTGRES_USER=postgres
POSTGRES_PASSWORD=<СИЛЬНЫЙ_ПАРОЛЬ>
DATABASE_URL=postgresql://postgres:<PASSWORD>@postgres:5432/employee_cabinet

# ======================================
# 📡 REDIS / КЭШ И SESSIONS
# ======================================
REDIS_URL=redis://redis:6379/0

# ======================================
# 📧 EMAIL / ПОЧТА
# ======================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=<APP_PASSWORD>
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=Employee Cabinet

# ======================================
# 🔑 JWT TOKENS
# ======================================
JWT_ALGORITHM=HS256
JWT_EXPIRATION_SECONDS=3600
REFRESH_TOKEN_EXPIRATION_DAYS=7

# ======================================
# 🛡️ SWAGGER / API DOCS SECURITY
# ======================================
# Используется для базовой аутентификации к документации
# Генерируется отдельно для каждого окружения!
SWAGGER_USERNAME=admin
SWAGGER_PASSWORD=<СИЛЬНЫЙ_ПАРОЛЬ>

# ======================================
# 🚀 ENVIRONMENT / ОКРУЖЕНИЕ
# ======================================
DEBUG=false
ENVIRONMENT=production
APP_NAME=Employee Cabinet
ENABLE_DOCS=false
DOCS_REQUIRE_AUTH=true
DOCS_ALLOWED_IPS=[]

# ======================================
# ⚡ RATE LIMITING
# ======================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# ======================================
# 🔒 ACCOUNT SECURITY
# ======================================
ACCOUNT_LOCKOUT_THRESHOLD=5
ACCOUNT_LOCKOUT_DURATION_MINUTES=15

# ======================================
# 🌐 CORS
# ======================================
CORS_ORIGINS=["http://localhost:3000","https://example.com"]
CORS_CREDENTIALS=true
CORS_METHODS=["GET","POST","PUT","DELETE"]
CORS_HEADERS=["Content-Type","Authorization"]